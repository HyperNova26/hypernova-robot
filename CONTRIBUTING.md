# 기여 가이드

## 기본 흐름

1. 작업 전에 Issue를 만들거나 기존 Issue를 연결합니다.
2. `main`에서 작업 브랜치를 만듭니다.
3. 변경과 함께 필요한 문서를 갱신합니다.
4. Pull Request를 열고 무엇을 바꿨고 어떻게 확인했는지 적습니다.
5. 리뷰 1명을 받고 병합합니다. `main`에 직접 push하지 않습니다.

## 브랜치와 커밋

브랜치는 `<종류>/<주제>` 형태로 만듭니다. 종류는 `feat`, `fix`, `docs`, `chore` 정도면 충분합니다.

예: `feat/lidar-bringup`, `docs/safety-checklist`

커밋은 한 가지 변경만 담고, 제목만 봐도 무엇을 했는지 알 수 있게 씁니다.

## Pull Request에 적을 것

- 해결하는 문제와 관련 Issue
- 바꾼 내용
- 어떻게 확인했는지, 확인하지 못한 부분은 무엇인지
- 하드웨어·안전·인터페이스에 영향이 있다면 그 내용

큰 변경은 구현 전에 Issue에서 먼저 상의합니다. 되돌리기 어려운 결정은 [ADR](./docs/decisions/)로 남깁니다.

## 파일과 데이터

소스, 설정, 문서, 작은 테스트 데이터는 Git으로 관리합니다. 빌드 산출물, 로그, rosbag, 포인트클라우드, 학습 weight는 넣지 않습니다. CAD·EDA 원본과 대형 바이너리는 [저장소 정책](./docs/development/repository-policy.md)을 먼저 확인합니다.

비밀키, 장비 계정, 개인 경로는 어떤 경우에도 커밋하지 않습니다.

## 라이선스

이 저장소에 기여하면 별도 합의가 없는 한 해당 경로에 적용되는 [라이선스 정책](./LICENSE.md)으로 기여물을 제공하는 데 동의한 것으로 봅니다. 외부 코드, 설계, 데이터 또는 미디어를 포함한다면 출처와 라이선스를 PR에 적고 재배포 권한을 확인합니다.

## 리뷰

기술적인 이견은 근거와 재현 방법으로 이야기합니다. 안전 관련 우려는 일정과 관계없이 바로 공유합니다.
