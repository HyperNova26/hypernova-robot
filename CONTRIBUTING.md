# 기여 가이드

## 기본 흐름

1. 작업 전에 Issue를 만들거나 기존 Issue에 붙입니다.
2. `main`에서 작업 브랜치를 땁니다.
3. 코드와 함께 관련 문서도 같이 고칩니다.
4. Pull Request에 무엇을 바꿨고 어떻게 확인했는지 씁니다.
5. 리뷰 한 명 받고 병합합니다. `main`에 직접 push는 안 됩니다.

## 브랜치와 커밋

브랜치 이름은 `<종류>/<주제>` 형태로 짓습니다. 종류는 `feat`, `fix`, `docs`, `chore` 정도면 충분합니다.

예: `feat/lidar-bringup`, `docs/safety-checklist`

커밋 하나에는 변경 하나만 담습니다. 제목만 읽고도 무슨 작업인지 짐작이 가야 합니다.

## Pull Request에 적을 것

- 해결하는 문제와 관련 Issue
- 바꾼 내용
- 어떻게 확인했는지, 확인 못 한 부분은 무엇인지
- 하드웨어·안전·인터페이스에 영향이 있다면 그 내용

큰 변경은 코드부터 쓰지 말고 Issue에서 먼저 이야기합니다. 되돌리기 어려운 결정은 [ADR](./docs/decisions/)로 남깁니다.

## 파일과 데이터

소스, 설정, 문서, 작은 테스트 데이터, 실행에 필요한 description asset은 Git으로 관리합니다. 빌드 산출물과 로그, rosbag, 포인트클라우드, dataset, 학습 weight는 넣지 않습니다. Fusion·KiCad 원본과 부품 선정 기록은 원래 쓰던 도구에 그대로 둡니다. 대형 바이너리나 외부 저장소를 붙이기 전에는 [저장소 정책](./docs/development/repository-policy.md)을 먼저 보세요.

비밀키, 장비 계정, 개인 경로는 어떤 경우에도 커밋하지 않습니다.

## 라이선스

이 저장소에 기여하면, 별도 합의가 없는 한 해당 경로의 [라이선스 정책](./LICENSE.md)으로 기여물을 제공하는 데 동의한 것으로 봅니다. 외부 코드나 설계, 데이터, 미디어를 가져왔다면 출처와 라이선스를 PR에 적고 재배포해도 되는지 확인합니다.

## 리뷰

기술적인 이견은 근거와 재현 방법으로 좁힙니다. 안전과 관련된 우려는 일정과 상관없이 바로 말합니다.
